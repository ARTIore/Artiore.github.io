import { Section } from '../ui/Section'

export function HomeMission() {
  return (
    <Section className="pt-4">
      <p className="max-w-4xl text-balance text-2xl font-medium leading-relaxed text-zinc-100 md:text-3xl">
        ARTIore exists to make invisible systems legible. Not by adding more noise. By reducing complexity into usable awareness.
      </p>
    </Section>
  )
}
